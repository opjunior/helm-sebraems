apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "odenir.fullname" . }}-test-connection"
  labels:
    {{- include "odenir.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "odenir.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
